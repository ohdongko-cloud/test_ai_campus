# dependencies
node_modules
.pnp
.pnp.js

# next.js
.next/
out/
build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# vercel
.vercel

# claude code — 로컬 설정은 제외하되, 공유 자산(스킬/커맨드/공용 설정)은 추적
.claude/*
!.claude/skills/
!.claude/commands/
!.claude/settings.json

# typescript
*.tsbuildinfo
next-env.d.ts

.vercel

# android (capacitor)
android/.gradle/
android/.idea/
android/local.properties
android/build/
android/app/build/
android/captures/
android/app/release/
android/app/debug/

# android signing — 절대 커밋 금지
android/keystore.properties
*.jks
*.keystore

# design handoff — 로컬 전용, public repo에 노출하지 않음
design_handoff_ai_campus/

# 로컬 프리뷰 목업 (임시)
.preview/

# PPT 생성 작업 디렉터리 (임시 — 제안서 덱 빌드용)
.pptx-build/
